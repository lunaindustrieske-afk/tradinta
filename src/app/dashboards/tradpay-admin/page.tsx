import { Card, CardHeader, CardTitle, CardDescription, CardContent } from "@/components/ui/card";

export default function TradPayAdminDashboard() {
    return (
        <Card>
            <CardHeader>
                <CardTitle>TradPay Admin Dashboard</CardTitle>
                <CardDescription>Technical oversight of payments, wallets, and TradCoin.</CardDescription>
            </CardHeader>
            <CardContent>
                <p>TradPay Admin specific components and data will go here.</p>
            </CardContent>
        </Card>
    );
}
