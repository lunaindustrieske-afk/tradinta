import { Card, CardHeader, CardTitle, CardDescription, CardContent } from "@/components/ui/card";

export default function DistributorDashboard() {
    return (
        <Card>
            <CardHeader>
                <CardTitle>Distributor Dashboard</CardTitle>
                <CardDescription>Buy in bulk, manage repeat orders, and limited shop privileges.</CardDescription>
            </CardHeader>
            <CardContent>
                <p>Distributor specific components and data will go here.</p>
            </CardContent>
        </Card>
    );
}
