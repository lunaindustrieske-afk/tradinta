import { Card, CardHeader, CardTitle, CardDescription, CardContent } from "@/components/ui/card";

export default function MarketingManagerDashboard() {
    return (
        <Card>
            <CardHeader>
                <CardTitle>Marketing Manager Dashboard</CardTitle>
                <CardDescription>Leads promotions, ambassador activities, and ad placements.</CardDescription>
            </CardHeader>
            <CardContent>
                <p>Marketing manager specific components and data will go here.</p>
            </CardContent>
        </Card>
    );
}
