import { Card, CardHeader, CardTitle, CardDescription, CardContent } from "@/components/ui/card";

export default function SupportDashboard() {
    return (
        <Card>
            <CardHeader>
                <CardTitle>Support Dashboard</CardTitle>
                <CardDescription>Handles user queries, support tickets, and disputes.</CardDescription>
            </CardHeader>
            <CardContent>
                <p>Support specific components and data will go here.</p>
            </CardContent>
        </Card>
    );
}
